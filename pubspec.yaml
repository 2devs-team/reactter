name: reactter
description: Reactter is a package that uses GetX as base to implement reactive behavior using some names and functionality we are familiarized from React Js.
version: 0.0.1-dev.4
homepage: https://2devs.io
license: MIT License

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.1

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter: null
